"""
backend/tools/anomaly_templates.py
=====================================
Template strings for AnomalyReport.explanation.

Per DESIGN_REVIEW_CHANGES.md #6, anomaly explanations are generated by
``str.format`` on these templates — never by an LLM call. This keeps
detection + explanation fully deterministic, free, and fast.

Wording is dataset/class-count-agnostic (no hardcoded "20%") since
``validation_collapse`` now uses a class-count-aware statistical test — see
``backend/tools/anomaly_detector.py``.

Requirements
------------
5.6  Anomaly_Detector provides a natural language explanation
"""

from __future__ import annotations

ANOMALY_TEMPLATES = {
    "outlier_detection": (
        "Validation {metric} of {value:.3f} is {z_score:.1f} std devs from "
        "the group mean ({mean:.3f}), within this dataset/configuration. "
        "This suggests numerical instability or incorrect configuration."
    ),
    "loss_divergence": (
        "Training loss increased over final 20% of epochs (from "
        "{initial_loss:.3f} to {final_loss:.3f}), indicating gradient "
        "explosion or learning rate too high."
    ),
    "validation_collapse": (
        "Validation accuracy of {accuracy:.1%} is not statistically "
        "distinguishable from random guessing ({baseline:.1%} for this "
        "{n_classes}-class task; z={z_score:.2f}), suggesting the model "
        "failed to learn or a data loading error."
    ),
}
